
export const AddCArtAction = (product) => {
    return async (dispatch) => {
        try {


            // if (localCart === null) {
            //     const ListProduct = [
            //         _product
            //     ];
            //     localStorage.setItem("CART", JSON.stringify(ListProduct))
            // } else {
            //     const index = localCart.findIndex(item => item.Product_ID === product.id)
            //     if (index !== -1) {
            //         localCart[index].Quantity += 1
            //     } else {
            //         localCart.push(_product)
            //     }
            //     localStorage.setItem("CART", JSON.stringify(localCart))
            // }
        } catch (error) {

        }
    }
}